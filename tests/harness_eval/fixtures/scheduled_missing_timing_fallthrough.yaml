name: scheduled_missing_timing_fallthrough
description: Recurring schedule without timing falls through to LLM loop
session_id: eval_missing_timing_01
user_text: monitor my account and post 3 times per day
orchestrator: true
mock_responses:
  - text: What time should I run the first post each day?
expect:
  orchestration_route: clarification_required
  response_fallthrough: true
  direct_return: false
  llm_calls_min: 1
