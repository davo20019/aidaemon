name: complex_goal_routing
description: Multi-step request enters orchestrator loop (may fall through to LLM)
session_id: eval_complex_01
user_text: Refactor the auth module, add tests, and deploy to staging
orchestrator: true
mock_responses:
  - text: "This requires three phases: inspect the auth module, refactor and test it, then deploy the verified result to staging."
expect:
  llm_calls_min: 1
  # The fixture verifies routing. Request keywords alone must not turn an
  # informative planning answer into a failed or partial execution claim.
  outcome: succeeded
  overall_min: 0.4
