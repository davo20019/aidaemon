name: basic_conversational
description: Simple conversational turn completes with harness eval snapshot
session_id: eval_basic_01
user_text: Hello there
expect:
  outcome: succeeded
  llm_calls_min: 1
  overall_min: 0.5
