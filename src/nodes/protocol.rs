//! Server-side re-export of the canonical, transport-neutral Node Protocol models.
//!
//! The standalone crate is also consumed by the Rust Node SDK and simulator;
//! C++ implementations conform through the canonical schema and golden fixtures.

pub use aidaemon_node_protocol::*;
