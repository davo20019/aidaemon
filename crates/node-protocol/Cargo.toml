[package]
name = "aidaemon-node-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Language-neutral AIdaemon Node Protocol v1 Rust models"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
